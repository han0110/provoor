.claude/
.env
.idea/
.local/
.playwright-cli/
benchmarkoor.db
/provoor

# Configs and results carry real hosts and addresses, so anything a template
# is materialized into stays out while the templates themselves stay tracked.
/cluster.yaml
/*-benchmark.yaml
/results/
examples/*.yaml
!examples/*.example.yaml

# The verifier library is a release asset scripts/fetch-verifier.sh downloads,
# too large to track and reproducible from its pinned version.
pkg/ereverifier/lib/

.claude/
.env
.idea/
.local/
.playwright-cli/
benchmarkoor.db
/provoor

# A materialized configuration carries real hosts, so only the templates stay
# tracked.
examples/*.yaml
!examples/*.example.yaml

# The verifier library is a release asset scripts/fetch-verifier.sh downloads.
internal/ereverifier/lib/
